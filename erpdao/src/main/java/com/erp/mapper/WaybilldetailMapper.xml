<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.mapper.WaybilldetailMapper">

    <!-- 根据sn查询信息 -->
    <select id="findBySn" parameterType="Long" resultType="WayBillDetail">
        SELECT * FROM waybilldetail WHERE su = #{su}
    </select>
</mapper>